/*
objects = lists of values similar to arrays
values are identified by keys instead of integers

var foodPreferences = {
    pizza: 'yum', 
    salad: 'gross'
};

*/

var pizza = {
    toppings: ['cheese', 'sauce', 'pepperoni'],
    crust: 'deep dish', 
    serves: 2
};

console.log(pizza);