/*
- function = block of code that atakes input, processes that input, and then proudces output

ex) 
function example (x) {
    return x * 2; 
}

call function using 
example(5);
- function assumes that example() will take a number a san arg and will return number multipled by 2
*/

function eat (food) {
    return food + ' tasted really good.';
}

console.log(eat('bananas'));