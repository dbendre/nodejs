/*
- can access and manipulate object properites -- keys and vluaes that object contains using method similar to arrays

var example = {
    pizza: 'ummy'
};
console.log(example['pizza']);
- code prints 'yummy' to terminal

alternately, can use dot notation to get identical results: 
    example.pizza;
    example['pizza'];
both lines of code yield the same result

*/

var food = {
    types: 'only pizza'
};

console.log(food.types);